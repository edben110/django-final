# Copiar a .env y rellenar antes del despliegue
SECRET_KEY=django-insecure-replace-me
DEBUG=False
ALLOWED_HOSTS=your-app.onrender.com
# URL de Postgres provista por Render, ejemplo:
# postgres://user:password@host:port/dbname
DATABASE_URL=

# Email (opcional)
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
